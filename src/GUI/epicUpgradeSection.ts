import { AdvancedDynamicTexture, Button, Rectangle, TextBlock, Control} from "@babylonjs/gui";
import { GUIFONT1 } from "../utils/CONSTANTS";
import { PlayMode } from "../scenes/playmode";

export class EpicUpgradeSection {

    constructor(name:string, instruction:string, lumensCost:number, maxNumberOfUpgrades:number, higherContainer:Rectangle | AdvancedDynamicTexture, guiVertPosition:number, scene:PlayMode, callback:any ) {

    }
}